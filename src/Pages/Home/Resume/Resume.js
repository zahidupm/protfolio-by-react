import React from 'react';
import useTitle from '../../../hooks/useTitle';

const Resume = () => {
    useTitle('Resume')
    return (
        <div>
            <h2>Resume</h2>
        </div>
    );
};

export default Resume;